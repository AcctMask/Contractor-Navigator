import type { FastifyInstance } from "fastify"

async function usersRoutes(_app: FastifyInstance) {
  // Legacy placeholder.
  // Auth / users / invitations are handled by src/routes/auth.ts
}

export default usersRoutes
